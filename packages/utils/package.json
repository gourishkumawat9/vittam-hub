{
  "name": "@vittamhub/utils",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic helpers shared by web and api (formatting, slugs, pagination math).",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@vittamhub/types": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
