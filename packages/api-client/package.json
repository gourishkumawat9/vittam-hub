{
  "name": "@vittamhub/api-client",
  "version": "0.1.0",
  "private": true,
  "description": "Typed fetch client + React Query hooks for the VittamHub API — the only place `fetch()` to our API should be called from.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "dependencies": {
    "@vittamhub/types": "workspace:*",
    "@tanstack/react-query": "^5.59.13"
  },
  "peerDependencies": {
    "react": "^18.3.1"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@vittamhub/config": "workspace:*",
    "@types/react": "^18.3.11",
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
