{
  "name": "@vittamhub/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types and Zod schemas — single source of truth for shapes crossing the web/API boundary.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint . --ext .ts --max-warnings 0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@vittamhub/config": "workspace:*",
    "eslint": "^8.57.1",
    "typescript": "^5.6.3"
  }
}
